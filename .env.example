# Optional webhook endpoint used by /api/contact and /api/data-deletion.
# Leave empty for local/demo deployments; the API routes will validate input and return success.
CONTACT_WEBHOOK_URL=

# Optional app marketplace URLs. Add real links when the mobile apps are published.
NEXT_PUBLIC_APP_STORE_URL=
NEXT_PUBLIC_GOOGLE_PLAY_URL=
